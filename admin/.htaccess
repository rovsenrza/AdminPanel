RewriteEngine On

# Pretty URLs inside /admin
RewriteRule ^login/?$ login.html [L]
RewriteRule ^$ index.html [L]

# Map /admin/{page} to /admin/{page}.html when file exists
RewriteCond %{REQUEST_FILENAME}.html -f
RewriteRule ^([^/]+)/?$ $1.html [L]
